### `CHANGELOG.md` (project root)

```markdown

## 0.0.1 - Initial scaffold

- Init Vite + React app
- Add Tailwind v4 via @tailwindcss/vite
- Create `DiveShopSite.jsx` placeholder ("Dive Connections - Tailwind OK")

## 0.0.2 - Hero + Header
- Added info banner and sticky header with phone-first CTAs (`tel:` : links)
- Built hero section (headline, pitch, buttons) and wired local hero image
- Fixed grid + aspect-ratio class typos, improved copy
